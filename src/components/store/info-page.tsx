import { useStore,type StoreConfig } from "@/context/store-context";export function InfoPage({title,kind}:{title:string;kind:keyof Pick<StoreConfig,"returns"|"terms"|"buying">}){const {config}=useStore();return <div className="mx-auto min-h-[55vh] max-w-3xl px-4 py-16"><p className="eyebrow">Información</p><h1 className="font-display text-5xl">{title}</h1><div className="prose mt-8 max-w-none"><p>{config[kind]}</p><p className="text-muted-foreground">Este contenido es referencial y puede editarse desde el panel administrador de la maqueta.</p></div></div>}
