import { createFileRoute } from "@tanstack/react-router";import { getCategoryName } from "@/data/products";import { CatalogPage } from "./catalogo";
export const Route=createFileRoute("/categoria/$slug")({head:({params})=>{const n=getCategoryName(params.slug);return{meta:[{title:`${n} — Valentino Dresses`},{name:"description",content:`Compra ${n.toLowerCase()} y recoge en nuestra tienda de Huancayo.`},{property:"og:title",content:`${n} — Valentino Dresses`},{property:"og:description",content:`Selección de ${n.toLowerCase()} disponible en tienda.`},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}},component:Category});function Category(){const {slug}=Route.useParams();return <CatalogPage category={slug}/>}
